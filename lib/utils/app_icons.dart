import 'package:flutter/material.dart';

class AppIcons {
  static const Icon monetization = Icon(Icons.monetization_on);
  static const Icon settings = Icon(Icons.settings);
  static const Icon add = Icon(Icons.add);
  static const Icon search = Icon(Icons.search, size: 39);
  static const Icon print = Icon(Icons.print, size: 39);
  static const Icon calculate = Icon(Icons.calculate, size: 39);
  static const Icon filter = Icon(Icons.filter_alt_outlined, size: 39);
  static const Icon archive = Icon(Icons.archive, size: 39);
  static const Icon unArchive = Icon(Icons.unarchive, size: 39);
  static const Icon report = Icon(
    Icons.insert_chart_outlined_rounded,
    size: 39,
  );
  static const Icon result = Icon(Icons.account_tree_outlined, size: 39);
  static const Icon remove = Icon(Icons.delete, size: 39);
  static const Icon arrowForqard = Icon(Icons.arrow_forward_ios);
}
